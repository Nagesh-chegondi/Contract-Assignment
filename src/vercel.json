{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api|assets|_next|_static|favicon.ico).*)",
      "destination": "/index.html"
    }
  ]
}
